# Stella API Configuration
# Document upload API (Cluster/FAT/Homepass Excel uploads)

# Base URL - own host, independent of ASTRI's API hosts
stella.api.base.url=http://172.17.52.160/astri-api-v2/v4

# Authentication - same credentials as ASTRI today (confirmed intentional at split time,
# not coincidental), but kept in a separate properties file so they can diverge later.
stella.username=smallworld
stella.password=Smallworld@2025!

# Timeouts (milliseconds)
stella.timeout.request=30000
stella.timeout.connection=10000
